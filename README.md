npm install (backend & frontend)

"npm run dev" to start the frontend
"npm start" to start the backend

Problems I encountered:

1. I had trouble connecting MSSQL.
   Solutions: Changed TCP Port of IPAll to 1433, Restart SQL Server (SQLEXPRESS), and dded sysadmin to the role of my user in MSSQL.
